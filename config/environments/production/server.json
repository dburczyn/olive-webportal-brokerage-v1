{
  "host": "localhost",
  "port": "${process.env.PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": "${process.env.PROXY_ENABLE || false}",
    "ssl": "${process.env.PROXY_SSL || true}",
    "host": "${process.env.PROXY_HOST || 'localhost'}" ,
    "port": "${process.env.PROXY_PORT || 443}"
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
